server:
  port: 8082
spring:
  application:
    name: data-work-svc
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml

  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true